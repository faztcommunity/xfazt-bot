/**
 * Cofiguración del bot.
 * @todo Este type puede cambiar
 */
export type bot_config = {
    /**
     * Prefijo del bot. Soporta espacios.
     */
    prefix: string;
    /**
     * ID de un servidor de Discrod
     */
    guild_id: string;
};
